# firmware/main/CMakeLists.txt
idf_component_register(
    SRCS
        "main.c"
        "display.c"      # vendored from clawd-tank — proven on this board
        "touch.c"        # vendored; event retargeted to APP_EVT_TOUCH
        "i2c_bus.c"      # vendored as-is
        "config_store.c" # NVS (pattern from clawd-tank, new keys)
        "ui.c"
        "ui_format.c"        # pure formatting / provider-resolution helpers
        "ui_render.c"        # LVGL widget construction + screen layout / draw
        "ui_screensaver.c"   # screensaver / backlight / idle-dimming logic
        "provider_icons.c"   # generated by scripts/build/gen-provider-icons.py
        "boot_splash.c"      # generated by scripts/build/gen-boot-splash.py
        "font_lemonmilk_48.c"
        "font_lemonmilk_36.c"
        "font_lemonmilk_24.c"
        "font_lemonmilk_23.c"
        "net_wifi.c"
        "provision.c"
        "upstash.c"
        "stats_model.c"
        "fetch.c"
        "screenshot.c"
        "led.c"
    INCLUDE_DIRS "."
    REQUIRES
        esp_lcd driver esp_timer nvs_flash freertos json
        led_strip
        esp_wifi esp_netif esp_event esp_http_client esp-tls
        esp_http_server mbedtls lwip esp_hw_support esp_system
)
